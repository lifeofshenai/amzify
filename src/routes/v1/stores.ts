import {Router} from "express";
// import WebhookController from "../../controllers/WebhookController";

const storeRouter: Router = Router();

// storeRouter.post("/paystack",);

export default storeRouter;
